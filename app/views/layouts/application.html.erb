<!DOCTYPE html>
<html>
  <head>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <%# <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <style>
      .navbar {
        background-color: #1e87bcff !important;
      }

      #logo {
       float: left;
       font-size: 1.7em;
       color: #fff;
       letter-spacing: -1px;
       font-weight: bold;
      }

      #search-colour {
        background-color: #c1d7d7ff !important;
        color: white !important;
      }

      #search-button {
        color: white !important;
        border-color: white !important;
      }
      #page-content{
        background-color: lightgrey;
      }
      #home-container {
        margin-top: 25px;
      }
      #home-text {
          background-color: rgba(0, 0, 0, 0.5);
          padding: 20px;
          border-radius: 10px;
          margin: 5 auto;
          margin-bottom: 50px;
      }
      .navbar .nav-link:hover {
        color: #ffcc00 !important;
      }
      #jumbotron-edit {
          display: flex;
          align-items: center;
          justify-content: center;
          height: 425px;
          text-align: center;
      }
      html, body {
        height: 100%;
        margin: 0;
        padding: 0;
      }
      #page-content{
        flex: 1 0 auto;
      }
      #sticky-footer{
        flex-shrink: none;
      }

      .flickr_pagination {
        text-align: center;
        padding: 0.3em;
        cursor: default; }
        .flickr_pagination a, .flickr_pagination span, .flickr_pagination em {
          padding: 0.2em 0.5em; }
        .flickr_pagination .disabled {
          color: #aaaaaa; }
        .flickr_pagination .current {
          font-style: normal;
          font-weight: bold;
          color: #ff0084; }
        .flickr_pagination a {
          border: 1px solid #dddddd;
          color: #0063dc;
          text-decoration: none; }
          .flickr_pagination a:hover, .flickr_pagination a:focus {
      border-color: #003366;
      background: #1e87bcff;
      color: white; }
        .flickr_pagination .page_info {
          color: #aaaaaa;
          padding-top: 0.8em; }
        .flickr_pagination .previous_page, .flickr_pagination .next_page {
          border-width: 2px; }
        .flickr_pagination .previous_page {
          margin-right: 1em; }
        .flickr_pagination .next_page {
          margin-left: 1em; }
    </style>
  </head>
  <body class="d-flex flex-column min-vh-100">
    <%= render 'layouts/navigation' %>
    <div id="page-content">
      <%= render 'layouts/messages' %>
      <%= yield %>
    </div>
    <footer id="sticky-footer" class="py-4 bg-dark text-white-50 mt-auto">
      <div class="container text-center">
        <small>Copyright &copy; <%= link_to "Bvoal Website", "https://bvokal.com/bevocal-summit/75D64D05/collaborative-social-responsibility", class: "text-white"  %></small>
      </div>
    </footer>
    <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>
  </body>
</html>
